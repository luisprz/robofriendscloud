//all caps in js is the standard. but is better to use a constant variable because with just the string if you misspell it you wont reecive the error when running the app.
//its nice also to have a file to see what your actions are.

export const CHANGE_SEARCH_FIELD = 'CHANGE_SEARCH_FIELD';